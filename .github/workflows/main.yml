name: My First Action

on: push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello GitHub Actions add CI workflow!"

        - name: Say Hello Step 2
        run: echo "Step 1"
