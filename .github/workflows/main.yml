name: Free AI Chat Bot

on:
  workflow_dispatch:

jobs:
  chat:
    runs-on: ubuntu-latest

    steps:
      - name: Generate Response
        run: |
          echo "🤖 AI Bot Response:"
          echo "GitHub Actions is a tool that automates workflows like testing, building, and deploying code."